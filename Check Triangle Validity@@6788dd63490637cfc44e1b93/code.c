#include <stdio.h>
#include <math.h>
int main(){
    int a,b,c;
    scanf("%d %d %d",&a, &b, &c);
    if(pow(0.5, pow(2, a) + pow(2, b) == pow(2, c))){printf("Valid");}
    else{printf("Invalid");}
    return 0;
}